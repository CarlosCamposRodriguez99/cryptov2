import styled from '@emotion/styled';

const Resultado = ({resultado}) => {

  const { PRICE, HIGHDAY, LOWDAY, CHANGEPCT24HOUR, IMAGEURL, LAST} = ;
  return (
    <div>
      <h1>Desde resultado</h1>
    </div>
  )
}

export default Resultado;